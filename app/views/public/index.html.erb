<%# ページング %>
<%= will_paginate @lists, :previous_label => 'Prev', :next_label => 'Next', :params => { :action => "index" } %><br />

<table border="0">
  <tr>
    <th><%= t("list.title") %></th>
    <th><%= t("list.mode") %></th>
    <th><%= t("list.user") %></th>
    <th><br /></th>
  </tr>

<% @lists.each do |list| %>
  <tr>
    <td><%=h list.title %></td>
    <td><%=h $mode[list.mode] %></td>
    <td><%= User.get_twitter_page( list.user_id ) %></td>
    <td class="menu"><%= link_to t("list.item"), { :action => "item_list", :id => list.id } %></td>
  </tr>
<% end %>
</table>

<%# ページング %>
<%= will_paginate @lists, :previous_label => 'Prev', :next_label => 'Next', :params => { :action => "index" } %><br />

<br />
